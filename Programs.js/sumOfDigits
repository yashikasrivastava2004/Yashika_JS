import promptSync from 'prompt-sync';
const prompt = promptSync();

let num = 1234;
let sum = 0; 

while (num > 0) {
    let digit = num % 10; 
    sum = sum + digit; 
    num = parseInt(num / 10); 
}

console.log("Sum of digits:", sum); 
